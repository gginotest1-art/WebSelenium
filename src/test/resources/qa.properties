environment=qa
app.name=RikBet QA

base.url=https://qa.rikbet.net/
login.url=${base.url}login
dashboard.url=${base.url}dashboard

browser=chrome
headless=true
browser.width=1920
browser.height=1080

implicit.wait=10
explicit.wait=15
page.load.timeout=30

log.level=DEBUG
screenshot.on.failure=true