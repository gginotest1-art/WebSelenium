# Test user credentials. Tests auto-pick up every active user — add a new
# block here, không cần đụng Java code.
#
# Schema: username, password (required); email, role, balance, tags (opt);
#         active defaults to true — set false to skip.

users:

  market4:
    username: markertest03
    password: markertest03
    email: market4@rikbet.net
    role: user
    balance: 1000
    tags: [smoke, regression]